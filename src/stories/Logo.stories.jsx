import React from 'react';
import Logo from '../stories/Logo';

export default {
  title: 'Atoms/Logo',
  component: Logo,
};

const Template = () => <Logo />;

export const Basic = Template.bind({});
